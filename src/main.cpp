#ifdef _WIN32
#include <windows.h>
#endif

#include <iostream>

// git restore .
// git fetch
// git pull

int main(int argc, char *argv[])
{
    std::cout << "Hello, World!" << std::endl;
    return 0;
}